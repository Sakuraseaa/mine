Runing
加入虚拟地址管理，完成execv 和 缺页中断，

LIST First
1. 进程的PCU使用的内存 和 内核栈使用的内存，分别都用动态申请的方式创建，不要混合。进程id用来保存进程地址
2. 把所有的全局变量，使用宏的方式创建
3. 解决 " sktest "标记处 的代码


Q:
1. 当线程shell和进程shell同时运行的时候，程序会报GP异常 请解决。与键盘程序读取有关. 2024-9-24 15:29
2. memdivmer_t 有三种没有使用到的计数，是什么意思
4. 实现进一步的进程调度器 使用 O(1)算法, SD/RSDL算法, 实现红黑树？
5. do_fork 执行逻辑和路径有些混乱，能不能更加结构化一些，清晰一些
6. 升级驱动程序，热插拔，驱动程序与内核程序禁止编译在一起
6. 重新建立makfile体系，在每个子目录下都建立上 makefile
2 bafhlst_t结构体中 head 单词好像无任何实际意义，容易产生误导 // block alloc free head list 内存块分配释放头链表
    bafhlst_t 在结构中只是头结构

Experience:
1. 在建立每一个结构体的时候，应该为其创建默认构造函数，初始化他。防止自己在代码中忘记初始化某个变量而引起的异常

Failed Result:
1. 设备框架升级失败，因为自己第一个是抽象能力不足，还有一个是学对设备了解的少